const db = require("../data/db-config");
const usersModel = require("./model");

// describe("users model", () => {
//     test("getAll", async () => {
//         const all = await usersModel.getAll();
//         expect(all).toHaveLength(5);
//     });
// });
// test("getTaskId", async () => {
//     const res = await usersModel.getTaskId(78);
//     expect(res.task).toBe("Study React");
// });
